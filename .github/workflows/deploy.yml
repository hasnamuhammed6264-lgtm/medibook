name: MediBook CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    name: Test Application

    env:
      SECRET_KEY: medibook-test-secret-key
      DATABASE_URL: sqlite:///medibook.db

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Test app starts correctly
      run: |
        python -c "
        from app import create_app, db
        app = create_app()
        with app.app_context():
            db.create_all()
            print('✅ App created successfully')
            print('✅ Database tables created')
        "

    - name: Check all routes exist
      run: |
        python -c "
        from app import create_app
        app = create_app()
        rules = [str(rule) for rule in app.url_map.iter_rules()]
        required = ['/', '/login', '/register', '/logout', '/doctors', '/dashboard']
        for route in required:
            if route in rules:
                print(f'✅ Route {route} exists')
            else:
                print(f'❌ Route {route} MISSING')
        "

  build:
    runs-on: ubuntu-latest
    needs: test
    name: Build Docker Image

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Build Docker image
      run: |
        docker build -t medibook:latest .
        echo "✅ Docker image built successfully"

  security:
    runs-on: ubuntu-latest
    needs: test
    name: Security Scan

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install security scanner
      run: pip install bandit

    - name: Run security scan
      run: |
        bandit -r app/ -f txt || true
        echo "✅ Security scan completed"
